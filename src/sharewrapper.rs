use std::sync::{Arc, Mutex};

//Shared resources for application
pub struct ShareWrapper{
    pub vec: Vec<u32>,
    pub sorting: bool,
    pub paused: bool,
}

impl ShareWrapper{
    pub fn getlength() {

    }
}